<?xml version="1.0" encoding="utf-8"?>
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"

    android:initialLayout="@layout/widget_main_layout"
    android:initialKeyguardLayout="@layout/widget_main_layout"

    android:minHeight="@dimen/widget_main_default_height"
    android:minWidth="@dimen/widget_main_default_width"
    android:updatePeriodMillis="0"

    android:minResizeHeight="@dimen/widget_main_min_resize_height"
    android:minResizeWidth="@dimen/widget_main_min_resize_width"

    android:previewImage="@drawable/widget_mini"

    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen|keyguard"

    tools:ignore="UnusedAttribute"
    android:autoAdvanceViewId="@id/widget_id_stack"
    />